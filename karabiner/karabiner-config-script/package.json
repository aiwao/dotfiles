{
  "name": "karabiner-config-script",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "generate": "bun run ./src/generate.ts",
    "apply": "bun run ./src/apply.ts",
    "start": "bun run generate && bun run apply",
  },
  "dependencies": {
    "inquirer": "^13.2.1",
    "zod": "^4.3.6"
  }
}
